package utli

const (
	// FileOnly : Check files only
	FileOnly int = 0
	// FolderOnly : Check folders only
	FolderOnly int = 1
	// FileAndFolder : Check both files and folders
	FileAndFolder int = 2
)
